// OBJECT.VALUES similar to OBJECT.ENTRIES, however, here we will get and array that contays the values of each item on the object

const countries = { IT : 'Italy', DK : 'Denmark' , JP : 'Japan' , CO : 'Colombia'}
console.log(Object.values(countries))

